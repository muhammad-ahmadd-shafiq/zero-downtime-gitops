apiVersion: v1
kind: Service

metadata:
  name: zero-downtime-stable
  namespace: gitops-demo

spec:
  selector:
    app: zero-downtime-gitops

  ports:
  - name: http
    port: 80
    targetPort: 5000